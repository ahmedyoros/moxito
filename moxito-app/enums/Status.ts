export enum RaceStatus {
  pending = 'pending',
  accepting = 'accepting',
  ongoing = 'ongoing',
  over = 'over',
}

export enum UserStatus {
  idle = 'idle',
  searching = 'searching',
  accepting = 'accepting',
  racing = 'racing',
}
