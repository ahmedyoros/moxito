export enum RaceStatus {
  pending = 'pending',
  accepting = 'accepting',
  pickingUp = 'pickingUp',
  ongoing = 'ongoing',
  over = 'over',
}

export enum UserStatus {
  idle = 'idle',
  searching = 'searching',
  accepting = 'accepting',
  racing = 'racing',
  arrived = 'arrived',
}
